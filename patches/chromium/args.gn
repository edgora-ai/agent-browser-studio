# Reproducible release defaults for the independent Chromium engine.
# Copy this file into out/AgentBrowserRelease/args.gn, add target_cpu when cross-building,
# then run `gn gen out/AgentBrowserRelease`.

is_debug = false
is_component_build = false
is_official_build = true
symbol_level = 0
blink_symbol_level = 0
v8_symbol_level = 0
enable_dsyms = false
chrome_pgo_phase = 0
use_remoteexec = false
use_siso = false
treat_warnings_as_errors = false

# Match the codec surface of a normal Chrome install. Redistributors are
# responsible for patent and distribution licensing in their jurisdictions.
proprietary_codecs = true
ffmpeg_branding = "Chrome"

# Widevine/DRM: enable the key-system plumbing and register a managed CDM
# passed via --widevine-cdm-path (patch 0045). The CDM itself is supplied at
# runtime, never bundled; the Google component updater is disabled so nothing
# phones home for the CDM.
enable_widevine = true
enable_widevine_cdm_component = false
ignore_missing_widevine_signing_cert = true
