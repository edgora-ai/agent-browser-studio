diff --git a/android_webview/browser/aw_content_browser_client.cc b/android_webview/browser/aw_content_browser_client.cc
index f270f57..5cfab25 100644
--- a/android_webview/browser/aw_content_browser_client.cc
+++ b/android_webview/browser/aw_content_browser_client.cc
@@ -305,7 +305,8 @@ void AwContentBrowserClient::OnNetworkServiceCreated(
         net::DnsOverHttpsConfig(),
         /*additional_dns_types_enabled=*/true,
         /*fallback_doh_nameservers=*/std::vector<net::IPEndPoint>(),
-        /*insecure_dns_via_platform_apis_enabled=*/true);
+        /*insecure_dns_via_platform_apis_enabled=*/true,
+        /*doh_via_proxy=*/false);
   }
 }
 
diff --git a/chrome/browser/net/stub_resolver_config_reader.cc b/chrome/browser/net/stub_resolver_config_reader.cc
index d855df0..e2ae118 100644
--- a/chrome/browser/net/stub_resolver_config_reader.cc
+++ b/chrome/browser/net/stub_resolver_config_reader.cc
@@ -39,6 +39,7 @@
 #include "net/dns/public/util.h"
 #include "services/network/public/mojom/host_resolver.mojom.h"
 #include "services/network/public/mojom/network_service.mojom.h"
+#include "third_party/blink/public/common/roxy_fingerprint_config.h"
 
 #if BUILDFLAG(IS_ANDROID)
 #include "base/android/android_info.h"
@@ -350,6 +351,21 @@ SecureDnsConfig StubResolverConfigReader::GetAndUpdateConfiguration(
                           .value_or(net::SecureDnsMode::kOff);
   }
 
+  // Agent Browser managed profiles force secure-only DNS over HTTPS through
+  // the configured proxy so DNS resolution stays consistent with the exit
+  // identity and never falls back to the host resolver. Pass-through profiles
+  // (no fingerprint config) keep stock Chromium behavior.
+  const blink::RoxyFingerprintConfig& roxy_config =
+      blink::RoxyFingerprintConfig::Get();
+  const bool managed_secure_dns =
+      roxy_config.enabled() && roxy_config.secure_dns_enabled() &&
+      !roxy_config.secure_dns_templates().empty();
+  if (managed_secure_dns) {
+    secure_dns_mode = net::SecureDnsMode::kSecure;
+    is_managed = true;
+    forced_management_mode = SecureDnsConfig::ManagementMode::kNoOverride;
+  }
+
   bool check_parental_controls = false;
   if (secure_dns_mode == net::SecureDnsMode::kSecure) {
     mode_details =
@@ -421,8 +437,18 @@ SecureDnsConfig StubResolverConfigReader::GetAndUpdateConfiguration(
   net::DnsOverHttpsConfig doh_config;
   std::vector<net::IPEndPoint> fallback_doh_nameservers;
   if (secure_dns_mode != net::SecureDnsMode::kOff) {
-    doh_config = net::DnsOverHttpsConfig::FromStringLax(
-        GetDnsOverHttpsConfigSource()->GetDnsOverHttpsTemplates());
+    if (managed_secure_dns) {
+      std::string joined_templates;
+      for (const std::string& server : roxy_config.secure_dns_templates()) {
+        if (!joined_templates.empty())
+          joined_templates.append(" ");
+        joined_templates.append(server);
+      }
+      doh_config = net::DnsOverHttpsConfig::FromStringLax(joined_templates);
+    } else {
+      doh_config = net::DnsOverHttpsConfig::FromStringLax(
+          GetDnsOverHttpsConfigSource()->GetDnsOverHttpsTemplates());
+    }
     if (ShouldUseDohFallback(secure_dns_mode,
                              *GetDnsOverHttpsConfigSource())) {
       mode_details =
@@ -445,7 +471,8 @@ SecureDnsConfig StubResolverConfigReader::GetAndUpdateConfiguration(
         secure_dns_mode, doh_config, additional_dns_query_types_enabled,
         fallback_doh_nameservers,
         net::features::IsDnsPlatformSupported() &&
-            base::FeatureList::IsEnabled(kChromeEnableDnsPlatform));
+            base::FeatureList::IsEnabled(kChromeEnableDnsPlatform),
+        managed_secure_dns);
   }
 
   return SecureDnsConfig(secure_dns_mode, std::move(doh_config),
diff --git a/content/renderer/renderer_main.cc b/content/renderer/renderer_main.cc
index e011fd9..ac71f19 100644
--- a/content/renderer/renderer_main.cc
+++ b/content/renderer/renderer_main.cc
@@ -55,6 +55,7 @@
 #include "services/tracing/public/cpp/trace_startup.h"
 #include "skia/ext/font_utils.h"
 #include "third_party/blink/public/common/features.h"
+#include "third_party/blink/public/common/roxy_fingerprint_config.h"
 #include "third_party/blink/public/platform/platform.h"
 #include "third_party/blink/public/platform/scheduler/web_thread_scheduler.h"
 #include "third_party/skia/include/core/SkFontMgr.h"
@@ -164,12 +165,23 @@ int RendererMain(MainFunctionParams parameters) {
   base::apple::ScopedNSAutoreleasePool* pool = parameters.autorelease_pool;
 #endif  // BUILDFLAG(IS_MAC)
 
+  const blink::RoxyFingerprintConfig& fingerprint_config =
+      blink::RoxyFingerprintConfig::Get();
+  if (fingerprint_config.enabled() &&
+      !fingerprint_config.languages().empty()) {
+    // Set the process-wide ICU locale before Skia, V8, Blink, or Worker
+    // initialization. macOS otherwise lets the host Cocoa locale override the
+    // managed renderer language even when --lang is present.
+    base::i18n::SetICUDefaultLocale(fingerprint_config.languages().front());
+  }
+
 #if BUILDFLAG(IS_CHROMEOS)
   // As the Zygote process starts up earlier than the browser process, it gets
   // its own locale (at login time for Chrome OS). So we have to set the ICU
   // default locale for the renderer process here.
   // ICU locale will be used for fallback font selection, etc.
-  if (command_line.HasSwitch(switches::kLang)) {
+  if (!fingerprint_config.enabled() &&
+      command_line.HasSwitch(switches::kLang)) {
     const std::string locale =
         command_line.GetSwitchValueASCII(switches::kLang);
     base::i18n::SetICUDefaultLocale(locale);
diff --git a/content/shell/browser/shell_content_browser_client.cc b/content/shell/browser/shell_content_browser_client.cc
index e7bb591..86c9306 100644
--- a/content/shell/browser/shell_content_browser_client.cc
+++ b/content/shell/browser/shell_content_browser_client.cc
@@ -817,7 +817,8 @@ void ShellContentBrowserClient::OnNetworkServiceCreated(
         net::DnsOverHttpsConfig(),
         /*additional_dns_types_enabled=*/true,
         /*fallback_doh_nameservers=*/{},
-        /*insecure_dns_via_platform_apis_enabled=*/false);
+        /*insecure_dns_via_platform_apis_enabled=*/false,
+        /*doh_via_proxy=*/false);
   }
 #endif
 }
diff --git a/net/dns/dns_config.cc b/net/dns/dns_config.cc
index 2c075f1..1ebf1d9 100644
--- a/net/dns/dns_config.cc
+++ b/net/dns/dns_config.cc
@@ -51,7 +51,8 @@ bool DnsConfig::EqualsIgnoreHosts(const DnsConfig& d) const {
          (allow_dns_over_https_upgrade == d.allow_dns_over_https_upgrade) &&
          (fallback_doh_nameservers == d.fallback_doh_nameservers) &&
          (should_perform_doh_fallback_upgrade ==
-          d.should_perform_doh_fallback_upgrade);
+          d.should_perform_doh_fallback_upgrade) &&
+         (doh_via_proxy == d.doh_via_proxy);
 }
 
 void DnsConfig::CopyIgnoreHosts(const DnsConfig& d) {
@@ -72,6 +73,7 @@ void DnsConfig::CopyIgnoreHosts(const DnsConfig& d) {
   allow_dns_over_https_upgrade = d.allow_dns_over_https_upgrade;
   fallback_doh_nameservers = d.fallback_doh_nameservers;
   should_perform_doh_fallback_upgrade = d.should_perform_doh_fallback_upgrade;
+  doh_via_proxy = d.doh_via_proxy;
 }
 
 base::DictValue DnsConfig::ToDict() const {
@@ -109,6 +111,7 @@ base::DictValue DnsConfig::ToDict() const {
   dict.Set("fallback_doh_nameservers", std::move(fallback_doh_nameserver_list));
   dict.Set("should_perform_doh_fallback_upgrade",
            should_perform_doh_fallback_upgrade);
+  dict.Set("doh_via_proxy", doh_via_proxy);
   return dict;
 }
 
diff --git a/net/dns/dns_config.h b/net/dns/dns_config.h
index add1443..51db864 100644
--- a/net/dns/dns_config.h
+++ b/net/dns/dns_config.h
@@ -114,6 +114,13 @@ struct NET_EXPORT DnsConfig {
   // If true, DoH fallback servers are copied into `doh_config` should be used
   // to send DNS queries over HTTPS.
   bool should_perform_doh_fallback_upgrade = false;
+
+  // Agent Browser managed profiles resolve DNS over HTTPS through the same
+  // proxy as normal traffic instead of bypassing it. When true, DoH requests
+  // do not set LOAD_BYPASS_PROXY so the proxy resolves the DoH endpoint and
+  // all DNS traffic stays consistent with the exit identity. Pass-through
+  // (unmanaged) builds keep stock Chromium behavior with this flag false.
+  bool doh_via_proxy = false;
 };
 
 }  // namespace net
diff --git a/net/dns/dns_http_attempt.cc b/net/dns/dns_http_attempt.cc
index 1f049eb..149114f 100644
--- a/net/dns/dns_http_attempt.cc
+++ b/net/dns/dns_http_attempt.cc
@@ -72,7 +72,7 @@ DnsHTTPAttempt::DnsHTTPAttempt(base::WeakPtr<ResolveContext> resolve_context,
                                bool use_post,
                                URLRequestContext* url_request_context,
                                RequestPriority request_priority,
-                               bool is_probe)
+                               bool is_probe, bool doh_via_proxy)
     : DnsAttempt(doh_server_index),
       is_probe_(is_probe),
       request_priority_(request_priority),
@@ -175,8 +175,13 @@ DnsHTTPAttempt::DnsHTTPAttempt(base::WeakPtr<ResolveContext> resolve_context,
   // Apply special policy to DNS lookups for for a DoH server hostname to
   // avoid deadlock and enable the use of preconfigured IP addresses.
   request_->SetSecureDnsPolicy(SecureDnsPolicy::kBootstrap);
+  // Managed Agent Browser profiles route DoH through the same proxy as normal
+  // traffic (see DnsConfig::doh_via_proxy), so the proxy resolves the DoH
+  // endpoint hostname and no DNS request leaks outside the exit identity.
+  // Probe attempts receive the same flag from the call site even though they
+  // hold no DnsSession reference.
   request_->SetLoadFlags(request_->load_flags() | LOAD_DISABLE_CACHE |
-                         LOAD_BYPASS_PROXY);
+                         (doh_via_proxy ? 0 : LOAD_BYPASS_PROXY));
   request_->set_disallow_credentials();
   request_->set_isolation_info(GetDohIsolationInfo());
 }
diff --git a/net/dns/dns_http_attempt.h b/net/dns/dns_http_attempt.h
index 39af55f..457c7ad 100644
--- a/net/dns/dns_http_attempt.h
+++ b/net/dns/dns_http_attempt.h
@@ -49,7 +49,7 @@ class NET_EXPORT_PRIVATE DnsHTTPAttempt : public DnsAttempt,
                  bool use_post,
                  URLRequestContext* url_request_context,
                  RequestPriority request_priority,
-                 bool is_probe);
+                 bool is_probe, bool doh_via_proxy);
   ~DnsHTTPAttempt() override;
 
   DnsHTTPAttempt(const DnsHTTPAttempt&) = delete;
diff --git a/net/dns/dns_transaction.cc b/net/dns/dns_transaction.cc
index 1f0d2e4..83ed259 100644
--- a/net/dns/dns_transaction.cc
+++ b/net/dns/dns_transaction.cc
@@ -131,6 +131,11 @@ void ConstructDnsHTTPAttempt(base::WeakPtr<ResolveContext> resolve_context,
                              bool is_probe) {
   DCHECK(url_request_context);
 
+  // Managed Agent Browser profiles route DoH through the same proxy as
+  // normal traffic, including probe attempts (probes hold no DnsSession
+  // reference, so the flag is captured here while the session is alive).
+  const bool doh_via_proxy = session && session->config().doh_via_proxy;
+
   std::unique_ptr<DnsQuery> query;
   if (attempts->empty()) {
     query =
@@ -148,7 +153,8 @@ void ConstructDnsHTTPAttempt(base::WeakPtr<ResolveContext> resolve_context,
   attempts->push_back(std::make_unique<DnsHTTPAttempt>(
       std::move(resolve_context), session, doh_server_index, std::move(query),
       doh_server.server_template(), gurl_without_parameters,
-      doh_server.use_post(), url_request_context, request_priority, is_probe));
+      doh_server.use_post(), url_request_context, request_priority, is_probe,
+      doh_via_proxy));
 }
 
 // ----------------------------------------------------------------------------
diff --git a/net/dns/host_resolver_manager_unittest.cc b/net/dns/host_resolver_manager_unittest.cc
index c7e32c6..e6749aa 100644
--- a/net/dns/host_resolver_manager_unittest.cc
+++ b/net/dns/host_resolver_manager_unittest.cc
@@ -8983,6 +8983,7 @@ TEST_F(HostResolverManagerDnsTest, SetDnsConfigOverrides) {
   const std::vector<IPEndPoint> fallback_doh_nameservers = {
       CreateExpected("8.8.8.8", net::dns_protocol::kDefaultPort)};
   overrides.fallback_doh_nameservers = fallback_doh_nameservers;
+  overrides.doh_via_proxy = true;
   overrides.clear_hosts = true;
 
   // This test is expected to test overriding all fields.
diff --git a/net/dns/public/dns_config_overrides.cc b/net/dns/public/dns_config_overrides.cc
index 74465d8..129cb5b 100644
--- a/net/dns/public/dns_config_overrides.cc
+++ b/net/dns/public/dns_config_overrides.cc
@@ -45,6 +45,7 @@ DnsConfigOverrides::CreateOverridingEverythingWithDefaults() {
   overrides.allow_dns_over_https_upgrade =
       defaults.allow_dns_over_https_upgrade;
   overrides.fallback_doh_nameservers = defaults.fallback_doh_nameservers;
+  overrides.doh_via_proxy = defaults.doh_via_proxy;
   overrides.clear_hosts = true;
 
   return overrides;
@@ -56,7 +57,7 @@ bool DnsConfigOverrides::OverridesEverything() const {
          attempts && doh_attempts && rotate && use_local_ipv6 &&
          dns_over_https_config && secure_dns_mode &&
          allow_dns_over_https_upgrade && fallback_doh_nameservers &&
-         clear_hosts;
+         doh_via_proxy && clear_hosts;
 }
 
 DnsConfig DnsConfigOverrides::ApplyOverrides(const DnsConfig& config) const {
@@ -98,6 +99,8 @@ DnsConfig DnsConfigOverrides::ApplyOverrides(const DnsConfig& config) const {
   if (fallback_doh_nameservers) {
     overridden.fallback_doh_nameservers = fallback_doh_nameservers.value();
   }
+  if (doh_via_proxy)
+    overridden.doh_via_proxy = doh_via_proxy.value();
   if (clear_hosts)
     overridden.hosts.clear();
 
diff --git a/net/dns/public/dns_config_overrides.h b/net/dns/public/dns_config_overrides.h
index dc30fe0..4c5ff90 100644
--- a/net/dns/public/dns_config_overrides.h
+++ b/net/dns/public/dns_config_overrides.h
@@ -62,6 +62,9 @@ struct NET_EXPORT DnsConfigOverrides {
   std::optional<SecureDnsMode> secure_dns_mode;
   std::optional<bool> allow_dns_over_https_upgrade;
   std::optional<std::vector<IPEndPoint>> fallback_doh_nameservers;
+  // When true, DoH requests are routed through the configured proxy instead of
+  // bypassing it (LOAD_BYPASS_PROXY is omitted). See DnsConfig::doh_via_proxy.
+  std::optional<bool> doh_via_proxy;
 
   // |hosts| is not supported for overriding except to clear it.
   bool clear_hosts = false;
diff --git a/services/network/network_service.cc b/services/network/network_service.cc
index 4718d88..5727f71 100644
--- a/services/network/network_service.cc
+++ b/services/network/network_service.cc
@@ -767,7 +767,8 @@ void NetworkService::ConfigureStubHostResolver(
     const net::DnsOverHttpsConfig& dns_over_https_config,
     bool additional_dns_types_enabled,
     const std::vector<net::IPEndPoint>& fallback_doh_nameservers,
-    bool insecure_dns_via_platform_apis_enabled) {
+    bool insecure_dns_via_platform_apis_enabled,
+    bool doh_via_proxy) {
   // Enable or disable the insecure part of DnsClient. "DnsClient" is the class
   // that implements the stub resolver.
   net::InsecureDnsMode mode;
@@ -793,6 +794,7 @@ void NetworkService::ConfigureStubHostResolver(
   overrides.allow_dns_over_https_upgrade =
       base::FeatureList::IsEnabled(features::kDnsOverHttpsUpgrade);
   overrides.fallback_doh_nameservers = fallback_doh_nameservers;
+  overrides.doh_via_proxy = doh_via_proxy;
   host_resolver_manager_->SetDnsConfigOverrides(overrides);
 
   const bool happy_eyeballs_v3_changed =
diff --git a/services/network/network_service.h b/services/network/network_service.h
index b5bbb11..1450bf5 100644
--- a/services/network/network_service.h
+++ b/services/network/network_service.h
@@ -183,7 +183,8 @@ class COMPONENT_EXPORT(NETWORK_SERVICE) NetworkService
       const net::DnsOverHttpsConfig& dns_over_https_config,
       bool additional_dns_types_enabled,
       const std::vector<net::IPEndPoint>& fallback_doh_nameservers,
-      bool insecure_dns_via_platform_apis_enabled) override;
+      bool insecure_dns_via_platform_apis_enabled,
+      bool doh_via_proxy) override;
   void DisableQuic() override;
   void SetUpHttpAuth(
       mojom::HttpAuthStaticParamsPtr http_auth_static_params) override;
diff --git a/services/network/network_service_unittest.cc b/services/network/network_service_unittest.cc
index abc40e9..2423081 100644
--- a/services/network/network_service_unittest.cc
+++ b/services/network/network_service_unittest.cc
@@ -730,7 +730,8 @@ TEST_F(NetworkServiceTest, DnsClientEnableDisable) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_TRUE(dns_client_ptr->CanUseInsecureDnsTransactions());
   EXPECT_EQ(net::SecureDnsMode::kOff,
             dns_client_ptr->GetEffectiveConfig()->secure_dns_mode);
@@ -741,7 +742,8 @@ TEST_F(NetworkServiceTest, DnsClientEnableDisable) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_FALSE(dns_client_ptr->CanUseInsecureDnsTransactions());
   EXPECT_EQ(net::SecureDnsMode::kOff,
             dns_client_ptr->GetEffectiveConfig()->secure_dns_mode);
@@ -752,7 +754,8 @@ TEST_F(NetworkServiceTest, DnsClientEnableDisable) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_FALSE(dns_client_ptr->CanUseInsecureDnsTransactions());
   EXPECT_EQ(net::SecureDnsMode::kAutomatic,
             dns_client_ptr->GetEffectiveConfig()->secure_dns_mode);
@@ -763,7 +766,8 @@ TEST_F(NetworkServiceTest, DnsClientEnableDisable) {
       *net::DnsOverHttpsConfig::FromString("https://foo/"),
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_FALSE(dns_client_ptr->CanUseInsecureDnsTransactions());
   EXPECT_EQ(net::SecureDnsMode::kAutomatic,
             dns_client_ptr->GetEffectiveConfig()->secure_dns_mode);
@@ -786,7 +790,8 @@ TEST_F(NetworkServiceTest, HandlesAdditionalDnsQueryTypesEnableDisable) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_TRUE(dns_client_ptr->CanQueryAdditionalTypesViaInsecureDns());
 
   service()->ConfigureStubHostResolver(
@@ -795,7 +800,8 @@ TEST_F(NetworkServiceTest, HandlesAdditionalDnsQueryTypesEnableDisable) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/false,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_FALSE(dns_client_ptr->CanQueryAdditionalTypesViaInsecureDns());
 }
 
@@ -815,7 +821,8 @@ TEST_F(NetworkServiceTest, HappyEyeballsV3EnableDisable) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_TRUE(service()->host_resolver_manager()->IsHappyEyeballsV3Enabled());
 
   service()->ConfigureStubHostResolver(
@@ -824,7 +831,8 @@ TEST_F(NetworkServiceTest, HappyEyeballsV3EnableDisable) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/false,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_FALSE(service()->host_resolver_manager()->IsHappyEyeballsV3Enabled());
 }
 
@@ -851,7 +859,8 @@ TEST_F(NetworkServiceTest, DnsOverHttpsEnableDisable) {
       kConfig1,
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_EQ(kConfig1, dns_client_ptr->GetEffectiveConfig()->doh_config);
 
   // Enable DNS over HTTPS for two servers.
@@ -861,7 +870,8 @@ TEST_F(NetworkServiceTest, DnsOverHttpsEnableDisable) {
       net::SecureDnsMode::kSecure, kConfig2,
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_EQ(kConfig2, dns_client_ptr->GetEffectiveConfig()->doh_config);
 }
 
@@ -886,7 +896,8 @@ TEST_F(NetworkServiceTest, AutomaticWithDohFallbackEnableDisable) {
       kConfig,
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_EQ(kConfig, dns_client_ptr->GetEffectiveConfig()->doh_config);
   EXPECT_TRUE(dns_client_ptr->GetConfigOverridesForTesting()
                   .fallback_doh_nameservers->empty());
@@ -903,7 +914,8 @@ TEST_F(NetworkServiceTest, AutomaticWithDohFallbackEnableDisable) {
       net::SecureDnsMode::kAutomatic, kConfig,
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/fallback_doh_nameservers,
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_EQ(
       dns_client_ptr->GetConfigOverridesForTesting().fallback_doh_nameservers,
       fallback_doh_nameservers);
@@ -916,7 +928,8 @@ TEST_F(NetworkServiceTest, AutomaticWithDohFallbackEnableDisable) {
       kConfig,
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   EXPECT_EQ(kConfig, dns_client_ptr->GetEffectiveConfig()->doh_config);
   EXPECT_TRUE(dns_client_ptr->GetConfigOverridesForTesting()
                   .fallback_doh_nameservers->empty());
@@ -946,7 +959,8 @@ TEST_F(NetworkServiceTest, DisableDohUpgradeProviders) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/true,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
 
   // Set valid DnsConfig.
   net::DnsConfig config;
@@ -1912,7 +1926,8 @@ TEST_F(NetworkServiceTestWithService, EnableDisableHappyEyeballsV3AndLoad) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/false,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   LoadURL(test_server()->GetURL("/echo"));
   EXPECT_EQ(net::OK, client()->completion_status().error_code);
 
@@ -1922,7 +1937,8 @@ TEST_F(NetworkServiceTestWithService, EnableDisableHappyEyeballsV3AndLoad) {
       /*dns_over_https_config=*/{},
       /*additional_dns_types_enabled=*/false,
       /*fallback_doh_nameservers=*/{},
-      /*insecure_dns_via_platform_apis_enabled=*/false);
+      /*insecure_dns_via_platform_apis_enabled=*/false,
+      /*doh_via_proxy=*/false);
   LoadURL(test_server()->GetURL("/echo"));
   EXPECT_EQ(net::OK, client()->completion_status().error_code);
 }
diff --git a/services/network/public/cpp/host_resolver_mojom_traits.cc b/services/network/public/cpp/host_resolver_mojom_traits.cc
index e444812..0dee076 100644
--- a/services/network/public/cpp/host_resolver_mojom_traits.cc
+++ b/services/network/public/cpp/host_resolver_mojom_traits.cc
@@ -198,6 +198,10 @@ bool StructTraits<DnsConfigOverridesDataView, net::DnsConfigOverrides>::Read(
     return false;
   }
 
+  if (data.doh_via_proxy()) {
+    out->doh_via_proxy = true;
+  }
+
   out->clear_hosts = data.clear_hosts();
 
   return true;
diff --git a/services/network/public/cpp/host_resolver_mojom_traits.h b/services/network/public/cpp/host_resolver_mojom_traits.h
index 106be2b..560dcbf 100644
--- a/services/network/public/cpp/host_resolver_mojom_traits.h
+++ b/services/network/public/cpp/host_resolver_mojom_traits.h
@@ -113,6 +113,10 @@ struct COMPONENT_EXPORT(NETWORK_CPP_HOST_RESOLVER)
     return overrides.fallback_doh_nameservers;
   }
 
+  static bool doh_via_proxy(const net::DnsConfigOverrides& overrides) {
+    return overrides.doh_via_proxy.value_or(false);
+  }
+
   static bool clear_hosts(const net::DnsConfigOverrides& overrides) {
     return overrides.clear_hosts;
   }
diff --git a/services/network/public/mojom/host_resolver.mojom b/services/network/public/mojom/host_resolver.mojom
index 50fbf3f..fb9f5a2 100644
--- a/services/network/public/mojom/host_resolver.mojom
+++ b/services/network/public/mojom/host_resolver.mojom
@@ -110,6 +110,10 @@ struct DnsConfigOverrides {
   // See net::DnsConfig::fallback_doh_nameservers.
   array<IPEndPoint>? fallback_doh_nameservers;
 
+  // When true, DoH requests are routed through the configured proxy instead of
+  // bypassing it. See net::DnsConfig::doh_via_proxy.
+  bool doh_via_proxy = false;
+
   // Whether to clear data read from the system HOSTS file.
   bool clear_hosts = false;
 };
diff --git a/services/network/public/mojom/network_service.mojom b/services/network/public/mojom/network_service.mojom
index aa20580..22a51d1 100644
--- a/services/network/public/mojom/network_service.mojom
+++ b/services/network/public/mojom/network_service.mojom
@@ -279,7 +279,8 @@ interface NetworkService {
                             DnsOverHttpsConfig dns_over_https_config,
                             bool additional_dns_types_enabled,
                             array<IPEndPoint> fallback_doh_nameservers,
-                            bool insecure_dns_via_platform_apis_enabled);
+                            bool insecure_dns_via_platform_apis_enabled,
+                            bool doh_via_proxy);
 
   // Disables QUIC for the NetworkService. Affects all existing NetworkContexts,
   // and all new ones that are created. Once called, QUIC cannot be re-enabled.
diff --git a/third_party/blink/renderer/platform/fonts/font_cache.cc b/third_party/blink/renderer/platform/fonts/font_cache.cc
index 0bc3c3d..4237199 100644
--- a/third_party/blink/renderer/platform/fonts/font_cache.cc
+++ b/third_party/blink/renderer/platform/fonts/font_cache.cc
@@ -33,7 +33,6 @@
 
 #include <limits>
 #include <memory>
-#include <optional>
 
 #include "base/byte_size.h"
 #include "base/debug/alias.h"
@@ -160,29 +159,31 @@ const FontPlatformData* FontCache::GetFontPlatformData(
     AlternateFontName alternate_font_name) {
   TRACE_EVENT0("fonts", "FontCache::GetFontPlatformData");
 
-  const FontFaceCreationParams* effective_creation_params = &creation_params;
-  std::optional<FontFaceCreationParams> managed_creation_params;
   if (creation_params.CreationType() == kCreateFontByFamily) {
     const RoxyFingerprintConfig& config = RoxyFingerprintConfig::Get();
     const std::string family = creation_params.Family().GetString().Utf8();
-    if (std::optional<std::string> managed_family =
-            config.ManagedGenericFontFamily(family)) {
-      managed_creation_params.emplace(AtomicString(managed_family->c_str()));
-      effective_creation_params = &*managed_creation_params;
-    } else if (!config.IsFontAllowed(family)) {
+    // Generic-vs-named is retained by FontFamily and resolved in
+    // FontSelector. At this lower layer a quoted concrete family such as
+    // "FangSong" must never be reinterpreted as the CSS `fangsong` generic.
+    // Blink marks its internal final fallback with kLastResort. It must remain
+    // available for layout after every page-supplied family is rejected; the
+    // name is not exposed as an installed font by FontFaceSet or Local Font
+    // Access.
+    if (alternate_font_name != AlternateFontName::kLastResort &&
+        !config.IsFontAllowed(family)) {
       return nullptr;
     }
   }
 
 #if !BUILDFLAG(IS_MAC)
-  if (effective_creation_params->CreationType() == kCreateFontByFamily &&
-      effective_creation_params->Family() == font_family_names::kSystemUi) {
+  if (creation_params.CreationType() == kCreateFontByFamily &&
+      creation_params.Family() == font_family_names::kSystemUi) {
     return SystemFontPlatformData(font_description);
   }
 #endif
 
   return font_platform_data_cache_.GetOrCreateFontPlatformData(
-      this, font_description, *effective_creation_params, alternate_font_name);
+      this, font_description, creation_params, alternate_font_name);
 }
 
 void FontCache::AcceptLanguagesChanged(const String& accept_languages) {
diff --git a/third_party/blink/renderer/platform/fonts/font_selector.cc b/third_party/blink/renderer/platform/fonts/font_selector.cc
index 7bb977d..e59e15d 100644
--- a/third_party/blink/renderer/platform/fonts/font_selector.cc
+++ b/third_party/blink/renderer/platform/fonts/font_selector.cc
@@ -29,7 +29,8 @@ AtomicString FontSelector::FamilyNameFromSettings(
   const RoxyFingerprintConfig& roxy_config = RoxyFingerprintConfig::Get();
   if (std::optional<std::string> managed_family =
           roxy_config.ManagedGenericFontFamily(
-              generic_family_name.GetString().Utf8())) {
+              generic_family_name.GetString().Utf8(),
+              generic_family.FamilyIsGeneric())) {
     return AtomicString(managed_family->c_str());
   }
   if (font_description.GenericFamily() != FontDescription::kStandardFamily &&
diff --git a/third_party/blink/renderer/platform/fonts/mac/font_cache_mac.mm b/third_party/blink/renderer/platform/fonts/mac/font_cache_mac.mm
index 6232b3c..0215851 100644
--- a/third_party/blink/renderer/platform/fonts/mac/font_cache_mac.mm
+++ b/third_party/blink/renderer/platform/fonts/mac/font_cache_mac.mm
@@ -374,12 +374,19 @@ bool IsSystemFontName(const AtomicString& font_name) {
 
 const SimpleFontData* FontCache::GetLastResortFallbackFont(
     const FontDescription& font_description) {
+  auto get_internal_last_resort = [&](const AtomicString& family) {
+    const FontPlatformData* platform_data = GetFontPlatformData(
+        font_description, FontFaceCreationParams(family),
+        AlternateFontName::kLastResort);
+    return platform_data ? FontDataFromFontPlatformData(platform_data)
+                         : nullptr;
+  };
+
   // FIXME: Would be even better to somehow get the user's default font here.
   // For now we'll pick the default that the user would get without changing
   // any prefs.
   const SimpleFontData* simple_font_data =
-      GetFontData(font_description, font_family_names::kTimes,
-                  AlternateFontName::kAllowAlternate);
+      get_internal_last_resort(font_family_names::kTimes);
   if (simple_font_data)
     return simple_font_data;
 
@@ -387,8 +394,7 @@ bool IsSystemFontName(const AtomicString& font_name) {
   // where the user doesn't have it, we fall back on Lucida Grande because
   // that's guaranteed to be there, according to Nathan Taylor. This is good
   // enough to avoid a crash at least.
-  return GetFontData(font_description, font_family_names::kLucidaGrande,
-                     AlternateFontName::kAllowAlternate);
+  return get_internal_last_resort(font_family_names::kLucidaGrande);
 }
 
 const FontPlatformData* FontCache::CreateFontPlatformData(
diff --git a/ui/display/mac/screen_utils_mac.mm b/ui/display/mac/screen_utils_mac.mm
index 7215ee0..60f4a3a 100644
--- a/ui/display/mac/screen_utils_mac.mm
+++ b/ui/display/mac/screen_utils_mac.mm
@@ -6,6 +6,21 @@
 
 namespace display {
 
+base::TimeDelta GetCGRefreshInterval(CGDirectDisplayID display_id) {
+  CGDisplayModeRef mode = CGDisplayCopyDisplayMode(display_id);
+  if (!mode) {
+    return base::Hertz(60);
+  }
+
+  const double refresh_rate = CGDisplayModeGetRefreshRate(mode);
+  CGDisplayModeRelease(mode);
+  if (refresh_rate <= 0.0) {
+    return base::Hertz(60);
+  }
+
+  return base::Hertz(refresh_rate);
+}
+
 NSScreen* GetNSScreenFromDisplayID(CGDirectDisplayID display_id) {
   for (NSScreen* screen in NSScreen.screens) {
     CGDirectDisplayID screen_display_id = kCGNullDirectDisplay;
